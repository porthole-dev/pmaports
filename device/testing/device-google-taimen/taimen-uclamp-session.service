[Unit]
Description=Raise user-<uid>.slice uclamp_min so the compositor+shell aren't starved by the CPU DVFS floor
After=graphical-session.target

[Service]
Type=oneshot
ExecStart=/usr/libexec/taimen-uclamp-session

[Install]
WantedBy=graphical-session.target
