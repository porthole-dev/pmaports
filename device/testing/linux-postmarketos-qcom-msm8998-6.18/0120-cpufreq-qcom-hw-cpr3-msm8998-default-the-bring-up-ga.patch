From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Giuseppe Maggio <jertlok@proton.me>
Date: Wed, 19 Aug 2026 13:27:58 +0200
Subject: [PATCH] cpufreq: qcom-hw, cpr3: msm8998: default the bring-up gates
 fully open

The SAW4 AVS fix makes the OSM deliver on both domains (silver 6.55x
for a 6.34x request, gold 8.00x for 7.87x, same-boot controlled), so
program everything by default.  The gates stay available for bisection.

Assisted-by: LLM
Signed-off-by: Giuseppe Maggio <jertlok@proton.me>
---
 drivers/cpufreq/qcom-cpufreq-hw.c | 3 ++-
 drivers/pmdomain/qcom/cpr3.c      | 3 ++-
 2 files changed, 4 insertions(+), 2 deletions(-)

diff --git a/drivers/cpufreq/qcom-cpufreq-hw.c b/drivers/cpufreq/qcom-cpufreq-hw.c
index fb9fbf22a1c8..fbf0f3fc21bf 100644
--- a/drivers/cpufreq/qcom-cpufreq-hw.c
+++ b/drivers/cpufreq/qcom-cpufreq-hw.c
@@ -299,7 +299,8 @@ static bool icc_scaling_enabled;
  * kernel command line and let the last surviving boot say where it died.
  * Zero programs nothing, which is the known-good control.
  */
-static int osm_setup_stop;
+/* Default 12 = all steps: cpufreq works since the SAW4 AVS fix. */
+static int osm_setup_stop = 12;
 module_param(osm_setup_stop, int, 0444);
 MODULE_PARM_DESC(osm_setup_stop,
 		 "stop OSM programming after step N (0: program nothing)");
diff --git a/drivers/pmdomain/qcom/cpr3.c b/drivers/pmdomain/qcom/cpr3.c
index ddab66901389..4c357f1d746a 100644
--- a/drivers/pmdomain/qcom/cpr3.c
+++ b/drivers/pmdomain/qcom/cpr3.c
@@ -297,7 +297,8 @@ struct cpr_desc {
  * half from the first register write so the last log line names the phase.
  * Zero touches no hardware at all, which is the known-good control.
  */
-static int cprh_init_stop;
+/* Default 5 = all phases: cpufreq works since the SAW4 AVS fix. */
+static int cprh_init_stop = 5;
 module_param(cprh_init_stop, int, 0444);
 MODULE_PARM_DESC(cprh_init_stop,
 		 "stop CPRh setup after phase N (0: no hardware access)");
