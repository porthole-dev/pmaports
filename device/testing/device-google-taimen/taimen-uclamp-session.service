[Unit]
Description=Raise phoc+phosh per-task uclamp_min so they aren't starved by the CPU DVFS floor
After=graphical-session.target

[Service]
Type=oneshot
ExecStart=/usr/libexec/taimen-uclamp-session

[Install]
WantedBy=graphical-session.target
