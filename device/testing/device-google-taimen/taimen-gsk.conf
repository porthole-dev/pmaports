# Every GTK4 app start probes Vulkan first; Turnip rejects the Adreno 540
# ("device unsupported") after touching the GPU, then GTK falls back to GL.
# Pin the GL renderer to skip the probe on every launch.
GSK_RENDERER=gl
